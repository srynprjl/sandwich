package initialize

/*
 * src/main.py
 * pyproject.toml
 */

func InitPython(projectData map[string]any) {
	// check if project already initialized
	// check if uv present
	// if not write everything from scratch and make it uv & pip compatible
}
